{
  "name": "habits-root",
  "private": true,
  "scripts": {
    "dev": "concurrently -n backend,frontend -c green,blue \"npm --prefix backend run dev\" \"npm --prefix frontend/habits run dev\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
